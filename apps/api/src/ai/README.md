# AI integration stub

A IA será um serviço externo Python/FastAPI integrado por HTTP. A spec `ai-prediction-contract` definirá payload, timeout e fallback; sua indisponibilidade nunca bloqueará a operação central.
